(module eliscript.core.seq
  (import "../../runtime/core/sequence.mjs"
          concat drop every filter find map remove reverse some take)

  (defun every? (predicate values)
    (every predicate values))

  (export concat drop every? filter find map remove reverse some take))
