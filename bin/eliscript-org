#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PROJECT_DIR=$(dirname -- "$SCRIPT_DIR")
: "${EMACS:=emacs}"

exec "$EMACS" --batch -Q \
  --script "$PROJECT_DIR/tools/org/eliscript-org-cli.el" \
  -- "$@"
