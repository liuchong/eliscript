# Examples

Examples are executable design probes. Each example should include source,
expected generated JavaScript, and the smallest environment needed to run it.

`basic/main.eli` is executable today. Compile it with `bin/eliscript` and run
the resulting module with Bun. `react-counter/` remains a planned example.
